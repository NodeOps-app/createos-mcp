openapi: 3.0.0
info:
  title: test
  version: 1.0.0
paths:
  /v1/foo:
    get:
      operationId: listFoo
      tags: [foo]
      description: should be stripped
      responses:
        '200':
          $ref: '#/components/responses/Ok'
components:
  responses:
    Ok:
      description: ok response
      content:
        application/json:
          schema:
            type: object
            properties:
              items:
                type: array
